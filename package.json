{
  "name": "tesseract-ocr-addon",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build:addon": "node-gyp rebuild --debug",
    "build:ts": "tsc -p tsconfig.json",
    "build": "npm run build:addon && npm run build:ts",
    "dev": "node dist/index.js"
  },
  "gypfile": true,
  "devDependencies": {
    "@types/node": "^22.0.0",
    "node-addon-api": "^8.5.0",
    "node-gyp": "^10.0.0",
    "typescript": "^5.6.0"
  }
}
